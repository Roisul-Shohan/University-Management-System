model Department {
  id   String @id @default(uuid())
  name String @unique
  code String @unique

  teachers Teacher[]
  programs Program[]
  courses  Course[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("departments")
}
